## React Tile -> Material UI Card

This project is under active development and is not stable, use at your own risk.

npm install --save react-tilecard

usage
import TileCard, { TileCardGrid } from "react-tilecard"
